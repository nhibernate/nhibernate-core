//------------------------------------------------------------------------------
// <auto-generated>
//     This code was generated by AsyncGenerator.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------


using System;
using System.Linq;
using NUnit.Framework;
using NHibernate.Linq;

namespace NHibernate.Test.NHSpecificTest.GH1712
{
	using System.Threading.Tasks;
	using System.Threading;
	[TestFixture]
	public class FixtureAsync : BugTestCase
	{
		protected override void OnSetUp()
		{
			using (var session = OpenSession())
			using (var tx = session.BeginTransaction())
			{
				session.Save(
					"GenericEntityWithGuid",
					new GenericEntity<Guid>(),
					Guid.Parse("093D2C0D-C1A4-42CB-95FC-1039CD0C00B6"));
				session.Save("GenericEntityWithLong", new GenericEntity<long>(), 42L);
				session.Save("GenericEntityWithString", new GenericEntity<string>(), "Bob l'éponge");
				session.Save("GenericEntityWithTimeSpan", new GenericEntity<TimeSpan>(), TimeSpan.FromDays(1));
				tx.Commit();
			}
		}

		protected override void OnTearDown()
		{
			base.OnTearDown();
			using (var session = OpenSession())
			using (var tx = session.BeginTransaction())
			{
				session.CreateQuery("delete from System.Object").ExecuteUpdate();
				tx.Commit();
			}
		}

		[Test]
		public async Task PerformingAQueryUsingIEquatableShouldNotThrowEqualsAsync()
		{
			using (var session = Sfi.OpenSession())
			{
				Assert.IsInstanceOf(
					typeof(GenericEntity<Guid>),
					await (QueryIdAsync("GenericEntityWithGuid", Guid.Parse("093D2C0D-C1A4-42CB-95FC-1039CD0C00B6"), session)));
				Assert.IsInstanceOf(
					typeof(GenericEntity<long>),
					await (QueryIdAsync("GenericEntityWithLong", 42L, session)));
				Assert.IsInstanceOf(
					typeof(GenericEntity<string>),
					await (QueryIdAsync("GenericEntityWithString", "Bob l'éponge", session)));
				Assert.IsInstanceOf(
					typeof(GenericEntity<TimeSpan>),
					await (QueryIdAsync("GenericEntityWithTimeSpan", TimeSpan.FromDays(1), session)));
			}
		}

		private Task<GenericEntity<TId>> QueryIdAsync<TId>(string name, TId id, ISession session, CancellationToken cancellationToken = default(CancellationToken)) where TId : IEquatable<TId>
			=> (from e in session.Query<GenericEntity<TId>>(name)
			    where e.Id.Equals(id)
			    select e).FirstOrDefaultAsync(cancellationToken);

		[Test]
		public async Task PerformingAQueryUsingIEquatableEntityShouldNotThrowEqualsAsync()
		{
			using (var session = Sfi.OpenSession())
			{
				Assert.IsInstanceOf(
					typeof(GenericEntity<Guid>),
					await (QueryEntityAsync("GenericEntityWithGuid", Guid.Parse("093D2C0D-C1A4-42CB-95FC-1039CD0C00B6"), session)));
			}
		}

		private Task<GenericEntity<TId>> QueryEntityAsync<TId>(string name, TId id, ISession session, CancellationToken cancellationToken = default(CancellationToken)) where TId : IEquatable<TId>
		{
			try
			{
				var entity = new GenericEntity<TId> { Id = id };
				return (from e in session.Query<GenericEntity<TId>>(name)
			        where e.Equals(entity)
			        select e).FirstOrDefaultAsync(cancellationToken);
			}
			catch (Exception ex)
			{
				return Task.FromException<GenericEntity<TId>>(ex);
			}
		}
	}
}
