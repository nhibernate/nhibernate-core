<?xml version="1.0"?>
<project
		name="NHibernate.Caches.Prevalence.Tests"
		default="build"
		description="Unit tests for NHibernate pluggable cache provider using Bamboo.Prevalence engine"
		xmlns="http://nant.sf.net/release/0.85/nant.xsd">

	<property name="nh.root.dir" value="${path::get-full-path('../../../../..')}" />
	<property name="project.dir" value="${path::get-full-path('../..')}"/>

	<include buildfile="${nh.root.dir}/contrib/build-common/common-csproj.xml" />

	<target name="init" depends="common.init">
		<assemblyfileset id="csproj.references">
			<include name="${output.dir}/*.dll"/>
			<include name="${project.dir}/lib/${framework.family}/*.dll"/>
			<include name="${nh.lib.dir}/*.dll"/>
			<include name="${nh.lib.family.dir}/*.dll"/>
			<include name="${nh.lib.framework.dir}/*.dll"/>
		</assemblyfileset>
	</target>
	
	<target name="build" depends="init common.compile-tests" />
	<target name="test" depends="build common.run-tests" />

	<!-- Intentionally empty -->
	<target name="generate-assemblyinfo" />
</project>
